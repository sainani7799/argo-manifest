apiVersion: v1
kind: Service
metadata:
  name: application-a
  namespace: application-a
spec:
  selector:
    app: application-a
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
