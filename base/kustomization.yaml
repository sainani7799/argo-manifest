apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: application-a

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: application-a
  managed-by: argocd

images:
  - name: application-a
    newTag: latest
