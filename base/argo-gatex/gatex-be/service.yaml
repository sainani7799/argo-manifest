apiVersion: v1
kind: Service
metadata:
  name: gatex-be
  namespace: argosai
spec:
  selector:
    app: gatex-be
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30081
