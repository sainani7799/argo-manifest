apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatex-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sainani7799/argo-manifest
    targetRevision: HEAD
    path: overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: gtx-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
