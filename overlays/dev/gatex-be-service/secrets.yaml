apiVersion: v1
kind: Secret
metadata:
  name: gatex-be-service-env
  namespace: gtx-dev
type: Opaque
stringData:
  # These should be managed by your secret management solution (sealed-secrets, vault, etc)
  DATABASE_USER: gatex_user
  DATABASE_PASSWORD: changeme_dev_password
  DATABASE_SSL: "false"
  REDIS_PASSWORD: ""
  API_KEY: dev-api-key-change-in-production
  JWT_SECRET: dev-jwt-secret-change-in-production
  WEBHOOK_SECRET: dev-webhook-secret
