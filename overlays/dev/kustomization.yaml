apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd-gatex-dev

resources:
  - gatex-ui-service
  - gatex-be-service

commonLabels:
  environment: dev
  managed-by: argocd
